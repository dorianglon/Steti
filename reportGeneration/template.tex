\documentclass{article}

\usepackage[top=0.5in, bottom=0.5in, left=0.5in, right=0.5in]{geometry}
\usepackage{enumitem}

\begin{document}
	\begin{center}
		\thispagestyle{empty}
		\large \textbf{ {{ name }} \\}
		\normalsize {{ dateRange }}    \\
		\hrulefill
	\end{center}

	\noindent 
	\textbf{\underline{USERS}} \\
	{% for user in users: %}
		\noindent 
		\textbf{ {{ user.username }} } 
		\hfill 
		{{ user.severity }} \\
		\textit{ {{ user.comment }} }
	{% endfor %}

\end{document}
